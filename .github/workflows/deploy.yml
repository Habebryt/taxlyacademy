# GitHub Actions workflow to deploy to Namecheap via FTP

name: 🚀 Deploy Website to Namecheap

on:
  push:
    branches:
      - live  # Triggers the workflow on a push to the live branch

jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files to server
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: ${{ secrets.SERVER_PATH }}
        # Optional: Add exclude rules if you don't want to upload certain files
        # exclude: |
        #   **/.git*
        #   **/.git*/**
        #   **/node_modules/**